Open index.html for the website. Click Launch Terminal to open the trading app.
For publishing, upload index.html and app.html to a static host. GitHub Pages publishes static HTML/CSS/JS from a repository. Live market data requires a secure backend; never expose broker API keys in the browser.
